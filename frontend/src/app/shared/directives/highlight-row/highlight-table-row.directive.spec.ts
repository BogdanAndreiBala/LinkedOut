import { HighlightTableRowDirective } from './highlight-table-row.directive';

describe('HighlightTableRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTableRowDirective();
    expect(directive).toBeTruthy();
  });
});
